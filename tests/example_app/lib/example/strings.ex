defmodule Example.Strings do
  def reverse_string(str), do: String.reverse(str)
  def uppercase_string(str), do: String.upcase(str)
  def lowercase_string(str), do: String.downcase(str)
  def concatenate_strings(str1, str2), do: str1 <> str2
end
