defmodule Example.Math do
  @doc """
  Adds two numbers together.

  ## Examples

      iex> Example.Math.sum(1, 2)
      3
  """
  def sum(a, b), do: a + b
  def multiply(a, b), do: a * b
  def divide(a, b), do: a / b
  def subtract(a, b), do: a - b
end
